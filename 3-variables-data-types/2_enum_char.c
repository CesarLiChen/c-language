#include <stdio.h>

int main() {

    enum primaryColor {red, yellow, blue};
    enum gender {male, female};

    enum primaryColor aPrColor;
    aPrColor = blue;

    char aChar = 'C';
    
    return 0;
}